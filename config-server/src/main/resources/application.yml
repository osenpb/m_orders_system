spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/osenpb/config-server-order-system.git

  output:
    ansi:
      enabled: ALWAYS
server:
  port: 8888

#   profiles:
#     active:
#     - native